services:
  - type: web
    name: guided-video-editor
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python guided_server.py
    envVars:
      - key: PORT
        value: 10000 